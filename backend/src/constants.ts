// expires in secons. Set to 10 days
export const JWT_EXPIRE = 10 * 24 * 60 * 60;

// refresh token expires in secons. Set to 30 days
export const JWT_REFRESH_EXPIRE = 30 * 24 * 60 * 60;

export const HASH_ROUNDS = 10;
